# Database Connection
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY&useSSL=true&allowPublicKeyRetrieval=true
spring.datasource.username=3SqiX64GydNd1RF.root
spring.datasource.password=DDqca5Y295fS2mJi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Settings (Auto-creates your tables!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Optional local properties import (for secrets and local overrides)
spring.config.import=optional:classpath:application-local.properties

# SendGrid Email Configuration (loaded from env var or application-local.properties)
sendgrid.api.key=${SENDGRID_API_KEY:}
sendgrid.from.email=${SENDGRID_FROM_EMAIL:spell7776@gmail.com}