package com.peertutor.api.service;

// Replaced by SessionLifecycleService — this class is intentionally disabled.
// SessionLifecycleService handles slot lifecycle with proper status transitions
// instead of hard-deleting slots.

@Deprecated
public class SlotCleanupService {
    // No-op — superseded
}
